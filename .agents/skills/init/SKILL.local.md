# Init Local Augmentations

## Additional item to include in AGENTS.md

Add item 4 to the "What to include" list from the base skill:

4. A "Validation after changes" section that tells AI agents to always run lint/check/test after making any code change. List the exact commands for this repo (e.g. `pnpm lint`, `pnpm test`, `npx biome check .`). Make it explicit that this is required before committing or pushing.
